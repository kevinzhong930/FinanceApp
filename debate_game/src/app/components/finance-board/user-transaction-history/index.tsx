import React,{useState, useEffect} from 'react';

interface UserTransactionHistoryProps {
    userId : number;
}

const user_transaction_history = ( {userId} : UserTransactionHistoryProps ) => {
    return(
        <div>

        </div>
    )
};

export default user_transaction_history;